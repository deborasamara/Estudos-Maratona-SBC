#include <bits/stdc++.h>

using namespace std;

int main()
{
    int N, S, M, L;
    int min_value = 0;
    

    // N = Qtd ovos
    // S = 6 ovos - Custo
    // M = 8 ovos - Custo
    // L = 12 ovos - Custo

    cin >> N >> S >> M >> L;

    // Cálculo S
    if(N <= 6)
    {

    }
    else if(N > 6)
    {

    }

    // Cálculo M
    if(N <= 8)
    {

    }
    else if(N > 8)
    {

    }

    // Cálculo L
    if(N <= 12)
    {

    }
    else if(N > 12)
    {

    }

    // Cálculo S - M

    // Cálculo S - M - L

    // Cálculo M - L

    // Cálculo L - S

    

    return 0;
}